  <style>
        body {
            font-family: Arial, sans-serif; /* Setting the default font */
            margin: 0;
            padding: 0;
            background-color: #f7f7f7; /* Setting background color */
        }

        header {
            background-color: #333; /* Navbar background color */
            color: #fff; /* Navbar text color */
            padding: 20px;
            text-align: center;
        }

        header h1 {
            margin: 0;
            font-size: 28px; /* Navbar title font size */
        }

        header ul {
            list-style-type: none;
            padding: 0;
        }

        header ul li {
            display: inline;
            margin-right: 20px;
        }

        header ul li a {
            color: #fff;
            text-decoration: none;
            font-size: 18px; /* Navbar link font size */
        }

        section {
            padding: 50px;
            text-align: center;
        }

        section h2 {
            margin-top: 0;
        }

        address {
            font-style: normal;
        }
    </style>